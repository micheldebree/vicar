function NearestNeighbour() {

    this.Downsize = function(pixelImage) {

        var pw = pixelImage.getWidth() / 320;
        var ph = pixelImage.getHeight() / 200;
        
        

    }
    
    this.calcPixel(pixelImage, x, y) {
        return pixelImage.peek(x,y);
    }

}
