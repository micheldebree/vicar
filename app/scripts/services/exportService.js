/*global angular */
angular.module('vicarApp').factory('exportService', function() {
  'use strict';


  //var koalaPic,
  //    converter = new KoalaPicture();

  // convert to koalapic
  // TODO: do this on demand, when user actually wants to save a koala pic
  //koalaPic = converter.convert(convertedImage);
  //$scope.mainImage = converter.toPixelImage(koalaPic, peptoPalette);
  //$scope.koalaDownloadLink = koalaPic.toUrl();


  return {

  };

});
